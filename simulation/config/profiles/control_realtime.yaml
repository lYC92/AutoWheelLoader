schema_version: 1
profile: control_realtime
description: Real-time vehicle control, localization, and planning loop.

clock:
  use_sim_time: true
  lockstep: false
  random_seed: 1001

physics:
  engine: dart
  step_size_s: 0.002
  target_rate_hz: 500
  minimum_average_realtime_factor: 0.9
  approved_fallback:
    step_size_s: 0.004
    target_rate_hz: 250
    internal_hydraulic_soil_substep_s: 0.001

sensors:
  lidar:
    enabled: true
    update_rate_hz: 10
  cameras:
    enabled_count: 1
    purpose: debug_only

gpu_policy:
  gazebo_rendering: true
  bev_inference: false
  chrono_dem: false
  mutually_exclusive: [bev_inference, chrono_dem]

validation_status: nominal

runtime_validation:
  date: 2026-09-04
  result: passed
  physics_step_hz: 500
  control_path: ros2_control
  average_realtime_factor: 0.990584
  lidar_average_rate_hz: 9.98277
  gpu_memory_peak_mib: 723
  evidence: /home/lyc/loader_sim_runtime/results/loader_control_profile_baseline.csv
